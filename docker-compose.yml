services:
  db:
    image: pgvector/pgvector:pg16
    container_name: bikeapp-db
    environment:
      POSTGRES_USER: bike
      POSTGRES_PASSWORD: bikepw
      POSTGRES_DB: bikeapp
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:

